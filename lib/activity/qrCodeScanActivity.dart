import 'package:flutter/material.dart';

class Qrcodescanactivity extends StatelessWidget {
  const Qrcodescanactivity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("This is qr scan activity")
        ],
      ),
    );
  }
}
