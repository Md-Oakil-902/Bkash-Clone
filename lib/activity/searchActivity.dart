import 'package:flutter/material.dart';

class SearchActivity extends StatelessWidget {
  const SearchActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("This is search  activity")
        ],
      ),
    );
  }
}
