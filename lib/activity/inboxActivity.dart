import 'package:flutter/material.dart';

class InboxActivity extends StatelessWidget {
  const InboxActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("This is Inbox  activity")
        ],
      ),
    );
  }
}
